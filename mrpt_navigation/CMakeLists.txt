cmake_minimum_required(VERSION 3.5)

project(mrpt_navigation)

# Find dependencies
find_package(ament_cmake REQUIRED)


# The following is needed for ROS 2's ament to generate the setup hooks
ament_package()